# v0.1.160 Corrective Acceptance

Status: implementation, Release, three-platform acceptance and cleanup passed; final record CI and acceptance marker pending.

## Why a Corrective Release

The complete v0.1.159 simplification is documented in [its implementation record](./v0.1.159-acceptance.md). Implementation `e5bd7f1417b2e56bf97ecd0ef2d41ec11318dcb2` passed CI `34117252357` and Release `34117847441`; all 15 assets, updater signatures, macOS/Tencent deployment and isolated official-helper replay passed. Final screenshot inspection then found horizontal scrolling at 320px with classic scrollbars: `innerWidth=320`, `clientWidth=305`, `scrollWidth=320`. The old headless browser configuration hid scrollbars and compared overflow to `innerWidth`, so it missed the defect. v0.1.159 is not accepted and its tag is not rewritten.

v0.1.160 removes the body minimum width. Chromium now retains classic scrollbars, the fixture waits for the settings form and asserts a real scrollbar gutter, and overflow uses `documentElement.clientWidth`. The original 320px light case failed before this CSS change; both 320px themes passed after it. The fresh browser gate also reproduced a late successful Probe save overwriting the next edit before its pending state rendered. Save completion now normalizes only the submitted draft; subsequent edits remain intact. The regression checks both the failed request payload and the retained input. No runtime API, notification/recovery algorithm, data format, dependency version or supported platform changes.

CI rendering also now installs CJK fonts, and an independent exact-tag AppImage workflow verifies a downloaded release with its checksum and a nonblank native four-workspace shell. The initial validation environment omitted Gdk introspection; that environment dependency is fixed in `23c726e0f8c23bd47eed7054b139995fb4976bf9`, whose four CI jobs passed in `34119754143`. Exact-tag v0.1.159 AppImage smoke then passed in `34119776523`. These validation changes do not rewrite any released binary.

Probe save buttons share an immediate in-flight guard, closing the duplicate-click window before React Query renders pending state. The guard holds through settlement, so the saved draft cannot be confused with another request. A delayed-response browser regression dispatches two same-turn save events and verifies one request and disabled inputs. Reloading a disabled Bark configuration now respects `enabled=false` even when a device key is configured. The browser fixture mirrors the server's nested save input and flattened notifications response, and verifies the visible saved toggle as well as the submitted flags.

## Verification

Fresh local gate passed: both Rust format checks, workspace 540 tests, Tauri 63 tests, both Clippy runs with warnings denied, frozen WebUI install, typecheck, 187 frontend tests, both build bases, 58 Chromium/WebKit regressions, installer/static contract/parity guards and webd package check. A first root test run during simultaneous compilation exceeded two Grok fixture timing limits; the complete same-test rerun passed after compilation finished, with no timeout or assertion weakened. Browser failures were investigated and fixed as described above.

The browser gate covers both themes, 1440x900, 1280x820, 390x844, 320px, 700/701/767/768 breakpoints, runtime system-theme changes, 200% zoom, long text, empty/error/loading states, keyboard menus, confirmation and cancellation. Action fixtures cover Codex search/filter/pagination/rename/archive/restore/copy, guarded Grok rename/deletion, Probe independent saves and update/job output. Read-only plans, questions, answers, tool history and notification ownership remain covered by domain/component and Rust tests. Destructive actions use isolated fixtures. No production task history was modified.

## Exact Release and Deployment

Implementation SHA: `19a43468e9d403451d7545abb526dde3cc32e41d`. All four jobs in [CI 34124094954](https://github.com/lich13/nexushub/actions/runs/34124094954) passed. Annotated tag `v0.1.160` resolves to that exact SHA. [Release 34124777209](https://github.com/lich13/nexushub/actions/runs/34124777209) passed all jobs and published [v0.1.160](https://github.com/lich13/nexushub/releases/tag/v0.1.160) with the original 15-asset contract.

All 15 files were downloaded and hashed; all six checksum sidecars and both updater signatures were verified, including cryptographic verification against the configured public key. `latest.json` contains only `darwin-aarch64` and `linux-x86_64`, exact-tag URLs and matching signatures. Headless webd is excluded. DMG and darwin updater App trees match; the installed App matches the DMG. AppImage/deb/rpm metadata identifies Linux x86_64 and version 0.1.160. Full hashes are in [assets.json](./v0.1.160-evidence/assets.json).

| Installed surface | Version | SHA-256 |
| --- | --- | --- |
| macOS App executable | 0.1.160 | `cd0a0e775e6d77fb253ea6a3f9d7cd3c2a1276011d00aa7a9d037bb64e010198` |
| macOS bundled and App Support helper | 0.1.160 | `9e31e1d3c36da9473ce2b934fe3c388eae246089e8398a47890ae7d198bbd0eb` |
| Tencent installed webd | 0.1.160 | `ba2b1235b12e6d8f60744a9f492849a0a3d51fc54c1619eac4f59405ce740dde` |

The official macOS Tauri App rendered all four workspaces, both system themes, current/latest version 0.1.160 and the collapsed navigation rail. Server-only security controls are absent. After closing the App, the official App Support monitor remained running (PID 20971), its latest scan was `ok` with no error, and it had no TCP listener. The system appearance was restored to its original dark setting. [macOS light screenshot](./v0.1.160-evidence/macos-settings-light.png), [dark screenshot](./v0.1.160-evidence/macos-settings-dark.png).

Tencent was installed from the exact-tag headless archive. `nexushub-webd` is active, loopback healthz returns `ok=true`, doctor reports database integrity `ok` and no discovery warnings. Config, authentication environment, systemd unit and nginx snippet hashes match the pre-install baseline. The existing authenticated Browser-plugin session verified the four workspaces at [the public WebUI](https://661313.xyz/nexushub/); no password reset, CAPTCHA bypass or session clearing was used. The fresh [45-route sweep](./v0.1.160-evidence/public-routes.json) has two expected 200 responses and 43 expected 404 responses, including all 33 retired RPC actions.

The [26 production rendered-style samples](./v0.1.160-evidence/cloud-rendered.json) have no overflow or contrast failures; their minimum sampled text contrast is 5.644:1. At 320px, actual content width is 305px with a classic scrollbar and scroll width is also 305px. Both themes and four workspaces were inspected. [320px light Settings](./v0.1.160-evidence/cloud-settings-light-320.png), [320px dark maintenance](./v0.1.160-evidence/cloud-maintenance-dark-320.png).

The first cache-disabled public reload reached the existing 3-second mount watchdog. A subsequent instrumented cold reload and a final normal reload mounted successfully: assets/RPC 200, no failed network requests, runtime exceptions or console errors. The route sweep independently encountered a transient TLS connection failure and passed after bounded retries. The initial cause is not proven; no reproducible application failure remained. Browser viewport, media emulation and caching were restored. Full non-secret state is in [deployment.json](./v0.1.160-evidence/deployment.json).

The Release job launched its actual AppImage under Xvfb before publication. The [native Linux smoke](./v0.1.160-evidence/linux-smoke.json) records a 1440x900 nonblank window, 4,281 edge pixels, 205 colors and a mounted desktop shell with all four workspace entries, with no Web login or server-only host copy. [Linux desktop screenshot](./v0.1.160-evidence/linux-desktop.png). Tencent remains headless.

The installed official helper on each host passed 12 isolated replay groups with 12 captured loopback Bark requests. Both main-turn bodies matched exact bytes, SHA-256, title and ownership. Repeats, answered questions, internal/unknown tasks, memory and control payloads were suppressed. Seven Goal states permitted only three restricted `status=active` sets, no `turn/*` calls and no duplicate scan notifications. [macOS replay](./v0.1.160-evidence/helper-macos.json), [Tencent replay](./v0.1.160-evidence/helper-tencent.json). This is official-binary replay, not a new phone-delivery or actual Codex automatic Stop-dispatch claim; those historical accepted boundaries are unchanged.

macOS retains its existing linker ad-hoc signing configuration. No Developer ID signing or notarization is claimed. The baseline and new App share the pre-existing strict resource-signature limitation; actual App launch and cryptographic updater verification passed.

## Measured Simplification

The original v0.1.158 source tree and final v0.1.160 production bundle were compared with the same Node/Vite/Chromium toolchain and mock fixture. Browser timings retain ten samples after two warmups. They exclude production network and Codex database variation. [Raw measurements and package counts](./v0.1.160-evidence/measurements.json), [before samples](./v0.1.160-evidence/measurement-before.json), [after samples](./v0.1.160-evidence/measurement-after.json).

| Measurement | v0.1.158 | v0.1.160 | Change |
| --- | ---: | ---: | ---: |
| CSS source bytes / lines | 58,698 / 1,982 | 26,673 / 338 | -54.6% / -82.9% |
| Built CSS bytes / gzip | 48,620 / 9,361 | 23,409 / 5,158 | -51.9% / -44.9% |
| Built server JS bytes / gzip | 468,038 / 142,246 | 477,163 / 145,144 | +1.9% / +2.0% |
| Direct dependency declarations: workspace / core / Tauri | 27 / 17 / 13 | 25 / 16 / 11 | five removed |
| Direct dependencies: webd / frontend runtime | 17 / 6 | 17 / 6 | unchanged |
| Lockfile packages: root / Tauri | 297 / 562 | 297 / 558 | 0 / -4 |
| Maintenance requests in 31 seconds | 16 | 1 | -93.8% |
| Closed jobs-history requests in 31 seconds | 6 | 0 | stopped |
| Browser list-ready median | 47.12ms | 50.43ms | +7.0% |
| Select-to-detail median | 62.28ms | 67.01ms | +7.6% |

No measured median exceeds the 10% regression threshold. Earlier v0.1.159 paired measurements were -1.6%/-2.6%, illustrating the small local timing variation; no startup speedup is claimed. The one remaining maintenance query is the visible logs DB status. JS growth comes with retained keyboard interaction, archive restoration and background terminal-job observation.

| Formal package bytes | v0.1.158 | v0.1.160 |
| --- | ---: | ---: |
| macOS DMG | 10,984,224 | 10,979,279 |
| darwin updater tar | 11,297,751 | 11,291,120 |
| Linux AppImage | 88,050,168 | 88,050,168 |
| Linux deb | 14,155,870 | 14,154,794 |
| Linux rpm | 14,160,415 | 14,155,093 |
| Headless webd tar | 6,228,983 | 6,228,439 |

Formal package size changes are negligible. Release logs show exactly one frontend build per macOS Tauri package and Linux Tauri package; the independent Linux headless build uses its own `/nexushub/` base. Independent gate/test builds remain. Source/CSS savings are not presented as package-size savings.

## Cleanup and Record Closure

Functional acceptance completed before cleanup. All evidence above was copied into this repository before deleting task staging. [Local cleanup](./v0.1.160-evidence/cleanup-local.json) resolved and removed 16 exact owned paths, totaling 9,801,203,292 logical bytes; [Tencent cleanup](./v0.1.160-evidence/cleanup-tencent.json) removed its exact task directory, totaling 54,461,777 logical bytes. Paths were rechecked absent. APFS clones/shared hardlinks mean these entry-size sums are not an exact physical free-space claim.

The local cleanup includes task-created Rust/Node/browser build dependencies, generated WebUI/Tauri output, downloads, isolated replay fixtures, temporary screenshots and both detached DMG staging trees. The verified v0.1.158 task rollback directory was 17,174,110 logical bytes (17,477,632 allocated bytes); no task rollback copy remains. Its old App archive, helper and cloud archive full hashes plus the exact-tag restoration source are retained in [rollback provenance](./v0.1.160-evidence/rollback-provenance.json). Shared pre-existing Cargo/pnpm caches, unknown `.DS_Store`, source/Git history, formal evidence, installed 0.1.160 binaries, production data/configuration/credentials and the official monitor are preserved.

The final evidence commit must pass its matching four-job CI before marking Goal 27 accepted. The subsequent acceptance-marker commit must also pass matching CI. Release `v0.1.160` remains pinned to the tested implementation SHA; documentation-only commits do not rewrite the tag or rebuild released assets.
