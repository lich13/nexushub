# v0.1.161 Runtime Correction

Status: implementation and verification in progress. Goal 27 remains open. v0.1.159/v0.1.160 tags are immutable; their completed theme, simplification and deployment evidence remains historical evidence, not acceptance of the runtime defects below.

## Reproduction and Change

The user supplied an affected Tencent Cloud task. A read-only query confirmed `history_mode=legacy`, the requested name in `threads.name`, and the old visible name in `threads.title`. The old mutation chose whichever name column existed, and NexusHub also preferred that column. Official Codex uses legacy `title` and paginated `name`, explaining the disagreement after rename. The installed cloud CLI is 0.153.4.

Rename now calls only `initialize`, `initialized`, `thread/name/set` and `thread/read` through a bounded local CLI process. Success requires the same task ID and requested native name on readback. Timeouts and mismatched readback fail visibly; there is no SQL fallback. The native operation maintains the name index and handles each history format. NexusHub's read model uses the corresponding native column. CLI discovery and bounded protocol framing reuse the existing internal client; Goal recovery and notification algorithms are unchanged. Linux retains auth, CSRF and audit; Tauri awaits the native operation without blocking its command dispatcher. No RPC/invoke DTO, configuration, database schema or public allowlist changes.

Official source at `openai/codex` commit `3d2ee51ca2d5db578f328aa75e20aa22c0197c9a` confirms history-format persistence and same-process notification broadcasting. Two isolated native app-server processes read each other's persisted names but do not exchange name notifications. Persistent native correctness is verified separately from live Desktop cache refresh; no claim is made that a new stdio process can notify an existing unrelated Desktop connection.

The native rename handler excludes archived tasks. The archived menu now disables rename with the shared `取消归档后可改名` tooltip; the backend rejects it before starting a CLI or changing metadata. The existing explicit restore action enables rename again. NexusHub never temporarily unarchives a task as a hidden part of rename.

The Grok report concerns new messages appearing late. The previous detail interval was 15 seconds, list interval 30 seconds, manual refresh omitted the detail, and long histories did not follow the latest output. Details now poll at 1 second while running and 2 seconds otherwise; lists poll every 5 seconds. A hidden mobile detail does not poll. New messages follow the bottom, while scrolling back preserves the reading position. Manual refresh includes the visible detail.

## Verification Record

- Browser regressions first failed on the old implementation: a new message did not appear within 3 seconds, and a long history stayed 4,354px from the bottom. After the fix, Chromium and WebKit both pass fresh-message, scroll-follow/history-preservation and hidden-detail polling cases (6 tests).
- Native CLI replay with isolated legacy and paginated fixtures passed. The requested native name, NexusHub readback and latest `session_index.jsonl` entry agree. Measured action times were 305ms and 46ms. Fixtures include the native history header marker; production task data is not copied into them.
- Protocol tests verify fixed methods/arguments, notification and unrelated-response correlation, mismatched ID/name rejection, bounded timeout with child reaping and no SQL fallback. A saturated initial run timed out during CLI discovery; the timeout fixture now resolves its fake CLI before measuring the protocol timeout.
- Complete local gate passed on the final implementation: both workspaces' formatting and Clippy with `-D warnings`, 544 root workspace tests, 63 Tauri tests, frozen frontend install, typecheck, 187 frontend tests, 64 Chromium/WebKit browser tests, server-base build, relative-base Tauri build, webd package `--check`, installation/contract/parity guards and `git diff --check`. The local host-mismatch package check is only a packaging smoke artifact and is not a deployable Linux release.
- Remaining required gates: exact-SHA CI/Release and 15 assets, formal macOS/Tencent deployment, the supplied task's real rename, native-file Grok freshness replay, Linux AppImage smoke, final evidence and cleanup.

## Protected Rollback

Before v0.1.161 installation, `/tmp/codex-01a07b11-778d-7fe2-a724-7f5549ececa8/v161/rollback` holds one verified v0.1.160 App archive and the exact-tag Linux server archive with its checksum. These remain protected until replacement acceptance.

| Artifact | Bytes | SHA-256 |
| --- | ---: | --- |
| `NexusHub-v0.1.160-installed.tar.gz` | 10936942 | `0cdd38ef4f0920763f0a86328c24843107e7b56eef8a6f2f30dec34b5f32b59d` |
| `nexushub-webd-linux-x86_64.tar.gz` | 6228439 | `c829d01c1f827fdbd48ea9f841038b164431cedb729d41c7315b59aca606b6ab` |

The App archive preserves executable SHA-256 `cd0a0e775e6d77fb253ea6a3f9d7cd3c2a1276011d00aa7a9d037bb64e010198` and bundled helper SHA-256 `9e31e1d3c36da9473ce2b934fe3c388eae246089e8398a47890ae7d198bbd0eb`. The installed App Support helper has that same hash and can be restored from the bundle. Tencent's pre-install binary hash is `ba2b1235b12e6d8f60744a9f492849a0a3d51fc54c1619eac4f59405ce740dde`.

Temporary native fixtures and diagnostics are under `/tmp/codex-01a07b11-778d-7fe2-a724-7f5549ececa8/v161`. Task-owned dependencies and build directories were recreated after the earlier v0.1.160 cleanup; that historical cleanup record does not claim they are currently absent.
