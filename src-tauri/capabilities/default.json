{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the NexusHub desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
